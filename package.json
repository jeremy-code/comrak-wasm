{
    "name": "comrak-wasm",
    "version": "0.0.7",
    "private": true,
    "license": "BSD-2-Clause",
    "scripts": {
        "prepare": "prek install",
        "lint": "cargo clippy --all-targets --all-features",
        "format": "oxfmt && cargo fmt --all",
        "format:check": "oxfmt --check && cargo fmt --all --check",
        "build": "wasm-pack build --target web --release --panic-unwind",
        "dev": "wasm-pack build --target web --dev",
        "test": "wasm-pack test --node",
        "ci:version": "changeset version && pnpm pkg get version | xargs cargo set-version",
        "ci:publish": "pnpm stage publish pkg"
    },
    "devDependencies": {
        "@changesets/cli": "^3.0.1",
        "@commitlint/cli": "^21.2.2",
        "@commitlint/types": "^21.2.0",
        "@j178/prek": "^0.4.14",
        "binaryen": "^132.0.0",
        "oxfmt": "^0.65.0",
        "wasm-pack": "^0.15.0"
    },
    "devEngines": {
        "packageManager": {
            "name": "pnpm",
            "version": "11.24.0",
            "onFail": "download"
        },
        "runtime": {
            "name": "node",
            "version": "^24.19.0",
            "onFail": "download"
        }
    }
}
